{
  "name": "beat-the-brewer-backend",
  "version": "1.0.0",
  "description": "Lambda functions for Beat the Brewer ratings site",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-bedrock-runtime": "^3.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"TODO: Add tests\" && exit 0"
  }
}
